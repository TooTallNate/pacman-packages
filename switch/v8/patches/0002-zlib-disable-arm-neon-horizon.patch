diff --git a/BUILD.gn b/BUILD.gn
index eff72b2..e08bfe6 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -70,7 +70,11 @@ source_set("zlib_common_headers") {
 # TODO(ritownsend@google.com): Optimizations temporarily disabled for
 # Windows on Arm MSVC builds, see http://crbug.com/v8/10012.
 use_arm_neon_optimizations =
-    (current_cpu == "arm" || current_cpu == "arm64") && !(is_win && !is_clang)
+    (current_cpu == "arm" || current_cpu == "arm64") &&
+    !(is_win && !is_clang) &&
+    # Horizon (Switch) lacks getauxval-based CPU feature detection that zlib's
+    # ARM CRC/NEON paths rely on; disable for now.
+    !is_horizon
 
 use_x86_x64_optimizations =
     (current_cpu == "x86" || current_cpu == "x64") && !is_ios
